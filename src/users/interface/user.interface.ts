export interface userRegisterInterface  {
        email: string;
        password: string;
        registration_device: string;
        app_id: string;
}

export interface userLoginInterface  {
    email: string;
    password: string;
    app_id: string;
}